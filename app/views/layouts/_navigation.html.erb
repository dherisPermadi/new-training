<div class="navigation">
  <% if user_signed_in? %>
    Logged in as <strong><%= current_user.email %></strong>
    <%= link_to 'Homework', homeworks_path, :class => 'button' %>
    <%= link_to 'Category', categories_path, :class => 'button' %>
    <%= link_to 'Edit profile', edit_user_registration_path, :class => 'button' %>|
    <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'button'  %>
  <% else %>
    <%= link_to "Sign up", new_user_registration_path, :class => 'button'  %>|
    <%= link_to "Login", new_user_session_path, :class => 'button'  %>
  <% end %>
</div>